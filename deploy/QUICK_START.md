# Быстрый старт: Создание репозитория и настройка секретов

## 🚀 Автоматический способ (рекомендуется)

Один скрипт создаст репозиторий, настроит git и добавит все секреты:

```bash
cd deploy
./create-repo-and-secrets.sh
```

Скрипт запросит:
1. Название репозитория (например: `boofmebel`)
2. GitHub username/org
3. Описание репозитория
4. Приватный/публичный
5. IP сервера
6. Пользователь SSH
7. Путь к SSH ключу

**Всё остальное сделает автоматически!**

## 📋 Что делает скрипт

1. ✅ Создаёт репозиторий в GitHub
2. ✅ Инициализирует git (если нужно)
3. ✅ Создаёт .gitignore (если нет)
4. ✅ Добавляет remote origin
5. ✅ Добавляет все секреты (SERVER_HOST, SERVER_USER, SERVER_SSH_KEY)
6. ✅ Делает первый коммит и push

## 🔧 Требования

### Вариант 1: GitHub CLI (проще)

```bash
# Установить
brew install gh  # macOS
# или с https://cli.github.com/

# Авторизоваться
gh auth login
```

### Вариант 2: GitHub API (требует токен)

1. Создать Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Права: `repo` (полный доступ)

2. Установить PyNaCl (для шифрования):
   ```bash
   pip install pynacl
   ```

## 📝 Пример использования

```bash
$ ./create-repo-and-secrets.sh

🚀 GitHub Repository & Secrets Setup
======================================

✅ GitHub CLI установлен и авторизован

Название репозитория (без owner): boofmebel
GitHub username/org: yourusername
Описание репозитория [BoofMebel API]: 
Приватный репозиторий? (y/n) [n]: n

📋 Настройка сервера:
IP или домен сервера: 123.45.67.89
Пользователь SSH [deploy]: deploy
Путь к приватному SSH ключу [~/.ssh/id_ed25519]: 

📋 Будет создано:
  Репозиторий: yourusername/boofmebel
  Описание: BoofMebel API
  Приватный: n
  SERVER_HOST: 123.45.67.89
  SERVER_USER: deploy
  SERVER_SSH_KEY: /Users/you/.ssh/id_ed25519

Продолжить? (y/n): y

🔄 Создание репозитория...
✅ Репозиторий создан
🔄 Инициализация git...
✅ Git инициализирован
🔐 Добавление секретов...
✅ SERVER_HOST добавлен
✅ SERVER_USER добавлен
✅ SERVER_SSH_KEY добавлен
📤 Первый коммит и push...
✅ Код отправлен в репозиторий

✅ Готово!
```

## 🔄 Для нескольких репозиториев

Запустите скрипт несколько раз с разными названиями:

```bash
# Первый проект
./create-repo-and-secrets.sh
# Ввести: boofmebel

# Второй проект
./create-repo-and-secrets.sh
# Ввести: another-project
```

## ✅ Проверка

После выполнения скрипта:

```bash
# Проверить секреты (если используете GitHub CLI)
gh secret list --repo yourusername/boofmebel

# Или через веб-интерфейс
# Репозиторий → Settings → Secrets and variables → Actions
```

## 🐛 Troubleshooting

### "GitHub CLI не установлен"
```bash
brew install gh
gh auth login
```

### "Permission denied"
- Проверить, что SSH ключ существует
- Или создать новый: `ssh-keygen -t ed25519`

### "Ошибка создания репозитория"
- Проверить права токена (должен быть `repo`)
- Проверить, что репозиторий с таким именем не существует

### "PyNaCl not found" (при использовании API)
```bash
pip install pynacl
```

## 🎯 Что дальше?

После создания репозитория и добавления секретов:

1. Настроить деплой на сервере (см. `DEPLOY_CHECKLIST.md`)
2. При каждом push в `main` будет автоматический деплой через GitHub Actions

