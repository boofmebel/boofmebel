# Текущий статус деплоя

## ✅ Что настроено

### GitHub Actions (deploy.yml)
- **main ветка** → деплой в `/var/www/boofmebel` (продакшн, порт 80)
- **dev ветка** → деплой в `/var/www/boofmebel-test` (тест, порт 9000)

### Автоматическое определение окружения
При push в ветку:
- `main` → `boofmebel` (prod)
- `dev` → `boofmebel-test` (test)

## ⚠️ Что нужно сделать на сервере

### Первоначальная настройка (один раз)

**1. Продакшн окружение:**
```bash
ssh user@server
cd /path/to/repo
./deploy/setup.sh boofmebel boofmebel.com prod
```

**2. Тестовое окружение:**
```bash
./deploy/setup.sh boofmebel boofmebel-test.com test
```

## 🔄 Как это работает сейчас

### После первоначальной настройки:

1. **Push в `dev` ветку:**
   - GitHub Actions деплоит в `/var/www/boofmebel-test`
   - Обновляет код из `dev` ветки
   - Перезапускает `boofmebel-test.service`
   - Доступ: `http://89.104.74.123:9000`

2. **Push в `main` ветку:**
   - GitHub Actions деплоит в `/var/www/boofmebel`
   - Обновляет код из `main` ветки
   - Перезапускает `boofmebel.service`
   - Доступ: `http://89.104.74.123` (порт 80)

## 📝 Важно

- **Первая настройка** - вручную через `setup.sh` (создаёт сервисы, Nginx конфиги)
- **Последующие деплои** - автоматически через GitHub Actions (только обновляет код)

## 🚀 Быстрый старт

```bash
# 1. Создать dev ветку (если ещё нет)
git checkout -b dev
git push -u origin dev

# 2. На сервере создать окружения (один раз)
./deploy/setup.sh boofmebel boofmebel.com prod
./deploy/setup.sh boofmebel boofmebel-test.com test

# 3. Дальше всё автоматически:
#    - Push в dev → тест (9000)
#    - Push в main → продакшн (80)
```

