# 🚀 Быстрый старт: Создание репозитория

## ✅ Всё готово! Запустите один из скриптов:

### Вариант 1: Простой запуск (рекомендуется)

```bash
cd deploy
./RUN_ME.sh
```

Этот скрипт:
- ✅ Установит зависимости (если нужно)
- ✅ Запустит создание репозитория
- ✅ Всё сделает автоматически

### Вариант 2: Прямой запуск Python скрипта

```bash
cd deploy
python3 create-repo-python.py
```

## 📋 Что нужно будет ввести:

1. **GitHub Personal Access Token**
   - Создайте здесь: https://github.com/settings/tokens
   - Права: `repo` (полный доступ)
   - Скопируйте токен (показывается только один раз!)

2. **Название репозитория** (например: `boofmebel`)

3. **GitHub username** (ваш логин)

4. **Описание** (можно оставить по умолчанию)

5. **Приватный/публичный** (y/n)

6. **IP сервера** (например: `123.45.67.89`)

7. **Пользователь SSH** (по умолчанию: `deploy`)

8. **Путь к SSH ключу** (по умолчанию: `~/.ssh/id_ed25519`)

## 🎯 Что сделает скрипт:

1. ✅ Создаст репозиторий в GitHub
2. ✅ Настроит git (инициализация, remote)
3. ✅ Создаст .gitignore (если нет)
4. ✅ Добавит все секреты (SERVER_HOST, SERVER_USER, SERVER_SSH_KEY)
5. ✅ Сделает первый коммит и push

## 🔄 Для нескольких репозиториев:

Запустите скрипт несколько раз с разными названиями:

```bash
# Первый проект
./RUN_ME.sh
# Ввести: boofmebel

# Второй проект
./RUN_ME.sh  
# Ввести: another-project
```

## ✅ После выполнения:

1. Проверьте репозиторий: https://github.com/yourusername/repo-name
2. Проверьте секреты: Репозиторий → Settings → Secrets and variables → Actions
3. Настройте деплой на сервере (см. `DEPLOY_CHECKLIST.md`)

## 🐛 Если что-то пошло не так:

- **"ModuleNotFoundError"** → Запустите: `pip install requests pynacl`
- **"Invalid token"** → Проверьте права токена (должен быть `repo`)
- **"SSH key not found"** → Создайте: `ssh-keygen -t ed25519`

---

**Готово к запуску!** 🚀

