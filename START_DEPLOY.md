# 🚀 Запуск автодеплоя - ОДИН СКРИПТ

## ✅ Всё готово! Запустите один скрипт:

```bash
./SETUP_DEPLOY.sh
```

## 📋 Что нужно будет ввести:

1. **GitHub username/org** (ваш логин)
2. **Название репозитория** (например: `boofmebel`)
3. **GitHub Personal Access Token**
   - Создайте: https://github.com/settings/tokens
   - Права: `repo` (полный доступ)
   - Скопируйте токен
4. **IP или домен сервера** (например: `123.45.67.89`)
5. **Пользователь SSH** (по умолчанию: `deploy`)

## 🎯 Что сделает скрипт:

1. ✅ Создаст репозиторий в GitHub
2. ✅ Добавит все секреты (SERVER_HOST, SERVER_USER, SERVER_SSH_KEY)
3. ✅ Настроит git remote
4. ✅ Отправит код в репозиторий
5. ✅ Настроит GitHub Actions для автодеплоя

## 🔄 После выполнения:

- При каждом `git push` в `main` будет автоматический деплой на сервер
- Проверить можно в: Репозиторий → Actions

## 📝 Следующий шаг:

Настроить сервер (см. `DEPLOY_CHECKLIST.md`):
- Установить PostgreSQL
- Настроить Nginx
- Запустить `deploy/setup.sh` на сервере

---

**Всё готово к запуску!** 🚀

