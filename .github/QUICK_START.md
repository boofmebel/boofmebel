# 🚀 Быстрый старт Git Workflow

## Простая стратегия (рекомендуется)

### 1. Создать ветку dev (если ещё нет)

```bash
git checkout -b dev
git push -u origin dev
```

### 2. Ежедневная работа

```bash
# Работаем в dev
git checkout dev
# ... делаем изменения ...
git add .
git commit -m "feat: описание изменений"
git push origin dev
```

**Результат:** Автоматический деплой на тест → `http://89.104.74.123:9000`

### 3. После проверки на тесте → в продакшн

```bash
git checkout main
git merge dev
git push origin main
```

**Результат:** Автоматический деплой на продакшн → `http://89.104.74.123`

## 📝 Правила коммитов

Используйте префиксы:
- `feat:` - новая функция
- `fix:` - исправление бага
- `refactor:` - рефакторинг
- `docs:` - документация
- `style:` - форматирование
- `test:` - тесты

Примеры:
```bash
git commit -m "feat: добавлена корзина покупок"
git commit -m "fix: исправлена ошибка расчета доставки"
git commit -m "refactor: оптимизирован запрос к БД"
```

## ⚠️ Важно

- **НЕ коммитить напрямую в main** (только через merge из dev)
- **Всегда проверять на тесте** перед merge в main
- **Использовать понятные сообщения коммитов**

