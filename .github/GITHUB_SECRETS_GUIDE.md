# Настройка GitHub Secrets

## 📋 Когда добавлять секреты

### Вариант 1: Несколько разных репозиториев (2+ проекта)

**Если у вас 2 разных репозитория в GitHub:**
- ✅ Секреты нужно добавить **в каждый репозиторий отдельно**
- Каждый репозиторий имеет свои собственные секреты

**Как добавить:**
1. Зайти в репозиторий 1: `Settings` → `Secrets and variables` → `Actions`
2. Добавить секреты для репозитория 1
3. Зайти в репозиторий 2: `Settings` → `Secrets and variables` → `Actions`
4. Добавить те же секреты для репозитория 2

**Секреты для каждого репозитория:**
- `SERVER_HOST` - IP или домен сервера (одинаковый для всех)
- `SERVER_USER` - пользователь SSH (обычно одинаковый)
- `SERVER_SSH_KEY` - приватный SSH ключ (можно один и тот же)

### Вариант 2: Один репозиторий, несколько сайтов на сервере

**Если один репозиторий, но несколько сайтов:**
- ✅ Секреты добавляются **один раз** в репозиторий
- При ручном запуске (workflow_dispatch) можно выбрать сайт для деплоя
- При автоматическом деплое (push) деплоится на сайт по умолчанию

## 🔑 Какие секреты нужны

### Обязательные секреты (для всех вариантов):

1. **SERVER_HOST**
   - IP адрес или домен вашего сервера
   - Пример: `123.45.67.89` или `server.example.com`
   - ⚠️ Без `http://` или `https://`

2. **SERVER_USER**
   - Пользователь для SSH подключения
   - Пример: `deploy` или `ubuntu`
   - Должен иметь права на sudo для перезапуска сервисов

3. **SERVER_SSH_KEY**
   - Приватный SSH ключ для доступа к серверу
   - Полный ключ включая заголовки:
     ```
     -----BEGIN OPENSSH PRIVATE KEY-----
     ...
     -----END OPENSSH PRIVATE KEY-----
     ```

## 📝 Пошаговая инструкция

### Шаг 1: Получить SSH ключ

**Если ключа нет, создать:**
```bash
ssh-keygen -t ed25519 -C "github-actions@server"
```

**Если ключ уже есть:**
```bash
# На вашем локальном компьютере или на сервере
cat ~/.ssh/id_ed25519
# или
cat ~/.ssh/id_rsa
```

Скопировать **весь вывод** (включая `-----BEGIN` и `-----END`)

### Шаг 2: Добавить публичный ключ на сервер

```bash
# На сервере
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавить публичный ключ в authorized_keys
echo "YOUR_PUBLIC_KEY_HERE" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### Шаг 3: Добавить секреты в GitHub

1. Зайти в репозиторий
2. `Settings` → `Secrets and variables` → `Actions`
3. Нажать `New repository secret`
4. Добавить каждый секрет:
   - Name: `SERVER_HOST`, Value: `ваш_IP_или_домен`
   - Name: `SERVER_USER`, Value: `deploy` (или ваш пользователь)
   - Name: `SERVER_SSH_KEY`, Value: `ваш_приватный_ключ`

### Шаг 4: Проверить подключение

```bash
# На вашем компьютере
ssh -i ~/.ssh/id_ed25519 deploy@ваш_сервер
```

Если подключается без пароля - всё готово!

## 🔄 Для нескольких репозиториев

Если у вас **2 разных репозитория**:

### Репозиторий 1 (например, boofmebel)
1. Зайти в репозиторий 1
2. `Settings` → `Secrets and variables` → `Actions`
3. Добавить все 3 секрета

### Репозиторий 2 (например, другой проект)
1. Зайти в репозиторий 2
2. `Settings` → `Secrets and variables` → `Actions`
3. Добавить те же 3 секрета (можно скопировать значения)

**Важно:** Значения секретов могут быть одинаковыми (один сервер), но добавлять нужно в каждый репозиторий отдельно.

## ✅ Проверка

После добавления секретов:
1. Зайти в `Actions` вкладку репозитория
2. Нажать `Deploy to Server` → `Run workflow`
3. Выбрать сайт (если workflow_dispatch)
4. Запустить

Если деплой прошёл успешно - секреты настроены правильно!

## 🐛 Troubleshooting

### Ошибка: "Permission denied (publickey)"
- Проверить, что приватный ключ скопирован полностью
- Проверить, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверить права: `chmod 600 ~/.ssh/authorized_keys`

### Ошибка: "Host key verification failed"
- Добавить сервер в known_hosts:
  ```bash
  ssh-keyscan ваш_сервер >> ~/.ssh/known_hosts
  ```

### Ошибка: "sudo: a password is required"
- Настроить sudo без пароля для пользователя:
  ```bash
  sudo visudo
  # Добавить строку:
  deploy ALL=(ALL) NOPASSWD: /bin/systemctl restart *, /bin/systemctl reload nginx
  ```

