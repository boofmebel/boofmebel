# Git Workflow для BoofMebel

## 🌿 Структура веток

```
main    → Продакшн (порт 80)
dev     → Тест (порт 9000)
feature/* → Временные ветки для разработки
```

## 📋 Стратегия работы

### Вариант 1: Простой (рекомендуется для начала)

**Для быстрой разработки:**
1. Работаем в `dev` → автоматический деплой на тест (порт 9000)
2. После проверки → merge в `main` → автоматический деплой на продакшн (порт 80)

**Команды:**
```bash
# Разработка в dev
git checkout dev
git add .
git commit -m "feat: новая функция"
git push origin dev  # → деплой на тест (9000)

# После проверки на тесте → в продакшн
git checkout main
git merge dev
git push origin main  # → деплой на продакшн (80)
```

### Вариант 2: С feature branches (для команды)

**Для организованной разработки:**
1. Создаём `feature/название` от `dev`
2. Разрабатываем → коммитим в feature
3. Merge в `dev` → тест (9000)
4. После проверки → merge в `main` → продакшн (80)

**Команды:**
```bash
# Создать feature ветку
git checkout dev
git checkout -b feature/новая-функция

# Разработка
git add .
git commit -m "feat: новая функция"
git push origin feature/новая-функция

# Merge в dev (через PR или напрямую)
git checkout dev
git merge feature/новая-функция
git push origin dev  # → деплой на тест

# После проверки → в main
git checkout main
git merge dev
git push origin main  # → деплой на продакшн
```

## 🚀 Автоматический деплой

### Текущая настройка (deploy.yml):
- `main` → деплой на продакшн
- Нужно добавить: `dev` → деплой на тест

## ✅ Рекомендация

**Для начала используйте Вариант 1 (простой):**
- `dev` для разработки и тестирования
- `main` для продакшна
- Коммиты сразу в `dev`, после проверки → в `main`

**Когда команда вырастет → переходите на Вариант 2 с feature branches.**

